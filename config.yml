title: Creating A Repository
description: A tutorial course on how to create a repository
template:
  repo: creating-a-repository-template
  name: creating-a-repository
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
